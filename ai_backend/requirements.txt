# AI Copilot for KiCad — Backend Dependencies
# Install: pip install -r requirements.txt

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# HTTP / LLM calls
requests>=2.31.0

# Data Validation
pydantic>=2.6.0

# .env file support
python-dotenv>=1.0.0

# Math (placement engine) — optional but recommended
numpy>=1.26.0

# HTTP client (for Ollama health checks)
httpx>=0.26.0

# ── Optional heavy deps (install manually only if needed) ─────────────────────
# Local GGUF inference (requires C++ compiler):
#   pip install llama-cpp-python
# Async file I/O:
#   pip install aiofiles
